export default defineAppConfig({
  ui: {
    notifications: {
      // Show toasts at the top right of the screen
      position: 'top-0 left-1/2 bottom-[unset] -translate-x-1/2'
    },
    primary: 'lime',
    gray: 'neutral',
  }
})